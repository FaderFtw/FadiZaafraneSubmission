spring:
  application:
    name: taskManager
  datasource:
    url: jdbc:mysql://db:3306/task_db
    username: root
    password: ""
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.MariaDBDialect

server:
  port: 8082

